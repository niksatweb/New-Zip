import { ButtonIcon } from "@/7_shared/ui/buttons"

export const AskQuestionButton = ({className}: {className?: string}) => {
  return (
    <ButtonIcon href="#" className={className}>Задать вопрос</ButtonIcon>

  )
}